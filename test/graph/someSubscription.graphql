query SomeSubscription {
    ChatTeam(id: $teamId) {
        orgs {
            repo(first: 100, offset: $offset, private: $isPrivate) {
                owner
                name
            }
        }
    }
}
