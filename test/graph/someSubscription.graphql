subscription
SomeSubscription(
$teamId: ID!,
$isPrivate: Boolean!,
$offset: Int!
) {
    ChatTeam(id: $teamId) {
        orgs {
            repo(first: 50, offset: $offset, private: $isPrivate) {
                owner
                name
                bla(test: $teamId) {
                    name
                }
            }
        }
    }
}